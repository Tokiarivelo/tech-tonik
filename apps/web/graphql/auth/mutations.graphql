mutation Login($data: LoginInput!) {
  login(data: $data) {
    token
    user {
      id
      email
      firstName
      lastName
      phone
      username
    }
  }
}

mutation Register($data: RegisterInput!) {
  register(data: $data) {
    id
    firstName
    username
  }
}
